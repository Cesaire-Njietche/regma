enum Location { local, network }

enum Entities { answers, questions, users, replies, courses, music, books }

enum FileTypes { video, audio, image, pdf }

enum MessageStatus { sending, sent, delivered, seen }

enum Error { uploadError }
